# Core dependencies
requests>=2.28.0
pydantic>=2.0.0
typing-extensions>=4.0.0

# Optional dependencies for enhanced functionality
tiktoken>=0.5.0
numpy>=1.21.0

# Web application support (installed with pip install -e .[web])
# flask>=2.0.0

# Browser support (installed with pip install -e .[browser])
# @mlc-ai/web-llm>=0.2.79

# Development dependencies (installed with pip install -e .[dev])
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=22.0.0
# isort>=5.0.0
# flake8>=5.0.0
# mypy>=1.0.0

# Streaming support (installed with pip install -e .[streaming])
# sseclient-py>=1.7.0 